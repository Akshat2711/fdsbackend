services:
  - type: web
    name: flask-app
    env: python
    buildCommand: ""
    startCommand: gunicorn --timeout 120 --workers 2 Server:app  # Adjust timeout and worker count
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 5000
